var searchFlags  = ['big', 'young', 'white'},
    pets         = [],              // Это массив. Полный объектов pet.
    numPets      = pets.lenght;

for( var i = 0; i < numPets; i++) {
  for( var c = 0; c <  search.lenght; c++) {
    if( pets[i][search[c]] == undefined) break;
  }
  if( c == search.lenght) {

  }
}
